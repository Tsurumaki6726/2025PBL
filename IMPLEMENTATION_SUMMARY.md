# News to Chat アプリケーション - 実装概要

## 完成したシステム

### アーキテクチャ図

\`\`\`
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  ブラウザ (React UI - Next.js)                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ • ニュース記事入力エリア                              │ │
│  │ • トーン選択（サイドバー）                            │ │
│  │ • チャット形式の会話表示                              │ │
│  │ • ローディングスピナー表示                            │ │
│  └───────────────────────────────────────────────────────┘ │
│                        ↕ HTTP API                          │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ FastAPI Server (Python)                              │ │
│  │ ┌─────────────────────────────────────────────────┐  │ │
│  │ │ POST /convert                                   │  │ │
│  │ │ • 記事テキストを受け取る                        │  │ │
│  │ │ • Qwenモデルで推論                             │  │ │
│  │ │ • 会話形式で返却                               │  │ │
│  │ └─────────────────────────────────────────────────┘  │ │
│  │                                                        │ │
│  │ ┌─────────────────────────────────────────────────┐  │ │
│  │ │ LLM (Qwen + Unsloth)                           │  │ │
│  │ │ • 記事を解析                                    │  │ │
│  │ │ • 会話形式の応答を生成                          │  │ │
│  │ │ • トーン制御                                    │  │ │
│  │ └─────────────────────────────────────────────────┘  │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
└─────────────────────────────────────────────────────────────┘
\`\`\`

---

## ディレクトリ構成

### フロントエンド（Next.js）

\`\`\`
frontend/
├── app/
│   ├── page.tsx                    # メインページ（会話フロー管理）
│   ├── layout.tsx                  # メタデータ設定
│   ├── globals.css                 # テーマ色定義、Tailwind設定
│   └── api/
│       └── convert/
│           └── route.ts            # FastAPI へのプロキシAPI
│
├── components/
│   ├── sidebar.tsx                 # トーン選択、リセットボタン
│   ├── article-input.tsx           # ニュース記事入力エリア
│   ├── convert-button.tsx          # 「変換する」ボタン、スピナー
│   └── chat-area.tsx               # チャット表示、メッセージフロー
│
├── package.json
├── tsconfig.json
└── .env.local
\`\`\`

### バックエンド（FastAPI）

\`\`\`
backend/
├── main.py                         # FastAPI サーバー本体
├── requirements.txt                # Python 依存パッケージ
├── data/
│   └── ehime_kiji_001.csv         # CSVデータ（オプション）
└── venv/                          # Python 仮想環境
\`\`\`

---

## 主要機能

### 1. ユーザーインターフェース

#### ✨ サイドバー
- **トーン選択**: フランク / 真面目 / 解説風
- **リセットボタン**: チャット履歴と入力をクリア
- **レスポンシブ**: モバイルでも使用可能

#### 📝 入力エリア
- 大きなテキストエリア
- リアルタイム入力
- プレースホルダーテキスト

#### ⚡ 変換ボタン
- FastAPI へリクエスト送信
- ローディング中はスピナー表示
- ボタン無効化（ユーザー操作防止）

#### 💬 チャット表示
- ユーザーメッセージ（青い背景）
- キャラクターA/B の会話（異なるスタイル）
- スムーズなアニメーション

---

### 2. API連携

#### Next.js API Route (`/api/convert`)

\`\`\`
Request:
{
  "article": "ニュース記事のテキスト...",
  "tone": "frank" | "serious" | "educational"
}

Response:
{
  "conversation": [
    { "role": "character_a", "content": "..." },
    { "role": "character_b", "content": "..." }
  ]
}
\`\`\`

#### FastAPI エンドポイント (`POST /convert`)

1. リクエスト受け取り
2. バリデーション（空文字列チェック）
3. LLM 推論実行
4. 会話形式に整形
5. JSON レスポンス

---

### 3. ステート管理（React）

\`\`\`typescript
// app/page.tsx
const [tone, setTone] = useState('frank')           // 選択中のトーン
const [chatHistory, setChatHistory] = useState([]) // チャット履歴
const [isLoading, setIsLoading] = useState(false)  // 処理中フラグ
const [articleText, setArticleText] = useState('') // 入力中の記事
\`\`\`

- **session_state 相当**: React の `useState` で状態管理
- **チャット履歴保持**: 複数回の会話をメモリ保持
- **ローディング状態**: ボタン無効化、スピナー表示制御

---

### 4. デザイン

#### カラースキーム（ライトモード）
- **背景**: #ffffff (白)
- **テキスト**: #1a1a1a (ほぼ黒)
- **アクセント**: #0066cc (青)
- **ボーダー**: #e8e8e8 (薄いグレー)

#### カラースキーム（ダークモード）
- **背景**: #0a0a0a (ほぼ黒)
- **テキスト**: #f5f5f5 (ほぼ白)
- **アクセント**: #3b82f6 (明るい青)
- **ボーダー**: #2a2a2a (濃いグレー)

#### レイアウト
- **デスクトップ**: 3 カラムレイアウト（サイドバー | 入力 | 出力）
- **タブレット**: 2 カラムレイアウト
- **モバイル**: スタック表示

---

## Pythonモデルの統合方法

### 現在の状態

`backend/main.py` の `convert_article_to_chat()` はモックアップ版です。

### 実装手順

#### Step 1: Google Colab のコードを準備

\`\`\`python
# Google Colab から以下をコピー
from unsloth import FastLanguageModel
import torch

# モデルのロード、生成ロジックなど
model, tokenizer = FastLanguageModel.from_pretrained(
    model_name="Qwen/Qwen2-7B",
    max_seq_length=2048,
    load_in_4bit=True,
)
\`\`\`

#### Step 2: `backend/main.py` に統合

\`\`\`python
# グローバルでモデルをロード（起動時のみ）
model = None
tokenizer = None

def load_model():
    global model, tokenizer
    if model is None:
        # ここにロード処理
        pass
    return model, tokenizer

def convert_article_to_chat(article_text: str, tone: str = "frank"):
    model, tokenizer = load_model()
    
    # トーンに応じたプロンプト設計
    tone_prompt = {
        "frank": "カジュアルで親しみやすく",
        "serious": "ビジネス向けの真面目なトーンで",
        "educational": "教育的で詳しい説明的に"
    }
    
    prompt = f"""
    以下のニュース記事を{tone_prompt[tone]}、
    キャラクターAとキャラクターBの対話形式に変換してください。
    
    記事:
    {article_text}
    
    出力形式:
    [A] キャラクターAのセリフ
    [B] キャラクターBのセリフ
    ...
    """
    
    # 推論実行
    inputs = tokenizer(prompt, return_tensors="pt").to(model.device)
    outputs = model.generate(**inputs, max_new_tokens=1024)
    response = tokenizer.decode(outputs[0])
    
    # 会話形式に解析
    conversation = parse_response(response)
    return conversation
\`\`\`

#### Step 3: パーサー関数を実装

\`\`\`python
def parse_response(response: str) -> list[dict]:
    """
    LLMの出力を会話形式に解析
    """
    conversation = []
    lines = response.split('\n')
    
    current_role = None
    current_content = ""
    
    for line in lines:
        if line.startswith('[A]'):
            # 前の会話を保存
            if current_role:
                conversation.append({
                    "role": current_role,
                    "content": current_content.strip()
                })
            current_role = "character_a"
            current_content = line.replace('[A]', '').strip()
        elif line.startswith('[B]'):
            if current_role:
                conversation.append({
                    "role": current_role,
                    "content": current_content.strip()
                })
            current_role = "character_b"
            current_content = line.replace('[B]', '').strip()
        else:
            if current_role:
                current_content += "\n" + line
    
    # 最後の会話を保存
    if current_role:
        conversation.append({
            "role": current_role,
            "content": current_content.strip()
        })
    
    return conversation
\`\`\`

---

## 実行フロー

### ユーザーアクション

\`\`\`
1. ユーザーが記事テキストを入力
2. トーンを選択（サイドバー）
3. 「変換する」ボタンをクリック
   ↓
4. スピナーが表示される
5. Next.js API Route が FastAPI に POST リクエスト
   ↓
6. FastAPI が LLM で推論
7. 会話形式の応答を返却
   ↓
8. React が チャット履歴に追加
9. スピナーが消える
   ↓
10. ユーザーが会話を見る
\`\`\`

---

## ローカル開発の起動手順

### ターミナル1: FastAPI

\`\`\`bash
cd backend
source venv/bin/activate
python main.py
\`\`\`

### ターミナル2: Next.js

\`\`\`bash
npm run dev
\`\`\`

### ブラウザ

`http://localhost:3000` にアクセス

---

## トラブルシューティング

| 問題 | 原因 | 解決方法 |
|------|------|--------|
| CORS エラー | FastAPI の CORS 設定不足 | `main.py` の CORS設定確認 |
| 502 Bad Gateway | FastAPI サーバーダウン | FastAPI を再起動 |
| モデル読み込み失敗 | メモリ不足 | Unsloth 量子化確認 |
| 推論が遅い | CPU 実行 | GPU 環境で実行 |

---

## 次のステップ

### 改善案

1. **キャッシング**: 同じ記事は推論結果をキャッシュ
2. **ストリーミング**: WebSocket で長時間推論に対応
3. **データベース**: チャット履歴を永続化
4. **ユーザー認証**: 複数ユーザーのサポート
5. **エラーハンドリング**: より詳細なエラーメッセージ

### デプロイ

1. **フロントエンド**: Vercel
2. **バックエンド**: Render, Railway, HF Spaces など

---

## 質問やサポート

実装に問題がある場合は、SETUP_GUIDE.md の「サポート」を参照してください。

Happy coding! 🚀
